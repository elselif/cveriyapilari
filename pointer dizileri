#include <stdio.h>
#include <string.h>

char *gunismi(char *gundizisi[],int uzunluk,int hangigun)
{
	if(hangigun >= 1 && hangigun <=7)
	{
		
		
		
		return gundizisi[hangigun-1];
	}
	else
	{
		return NULL;
	}
}

int main()
{
	char *gunler[7]= {"pzt","sali","çrş","prş","cuma","cmt","pazar"};
	
	
	char *p = gunismi(gunler,7,5);
	
	if(p == NULL)
	{
		printf("null");
	}
	else
	{
		printf("%s",p);
	}
}
